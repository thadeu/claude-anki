---
description: anki:list — list every English lesson, newest first
---

List the saved English lessons.

1. List `~/.claude/anki-data/lessons/*.md`, sorted newest first by the date prefix.
2. For each, show the date and a readable title (from the filename or the first `#` heading).
3. End with a count and a hint: `/anki:today` for a new one, `/anki:review <topic>` to drill an old one.

Keep it compact — a clean table or list, no lesson contents.
